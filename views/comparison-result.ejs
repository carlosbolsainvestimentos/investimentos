<!DOCTYPE html>
<html>
  <head>
    <title>Resultado da Comparação</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link rel="stylesheet" href="/css/style.css" />
  </head>
  <body class="container my-4">
    <h1>Resultado da Comparação</h1>
    <p>Cotação oficial usada (via API): <strong><%= cotacaoOficial.toFixed(4) %></strong></p>
    <p>Valor em Reais: <strong><%= valorReais %></strong></p>

    <table class="table table-bordered table-striped">
      <thead>
        <tr>
          <th>Plataforma</th>
          <th>Cotação Ajustada (R$)</th>
          <th>IOF (R$)</th>
          <th>Dólares Recebidos</th>
        </tr>
      </thead>
      <tbody>
        <% results.forEach(r => { %>
        <tr>
          <td><%= r.name %></td>
          <td><%= r.cotacaoAjustada %></td>
          <td><%= r.valorIOF %></td>
          <td><%= r.dolaresComprados %></td>
        </tr>
        <% }) %>
      </tbody>
    </table>

    <a href="/comparison" class="btn btn-primary">Nova Simulação</a>
    <a href="/dashboard" class="btn btn-secondary">Voltar</a>
  </body>
</html>
