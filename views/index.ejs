<!DOCTYPE html>
<html>
  <head>
    <title>Dashboard</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link rel="stylesheet" href="/css/style.css" />
  </head>
  <body class="container my-4">
    <h1><%= message %></h1>

    <div class="my-3 p-3 bg-light">
      <h2>Resumo Financeiro</h2>
      <p><strong>Saldo em dólares:</strong> <%= saldoDolares.toFixed(2) %></p>
      <p><strong>Preço médio:</strong> R$ <%= precoMedio.toFixed(4) %></p>
      <p><strong>Total investido (R$):</strong> <%= totalInvestido.toFixed(2) %></p>
      <p><strong>Total recebido (R$):</strong> <%= totalRecebido.toFixed(2) %></p>
      <hr />
      <p><strong>Cotação atual usada:</strong> R$ <%= cotacaoAtual %></p>
      <p><strong>Valor se vendesse tudo agora (R$):</strong> <%= valorAtual.toFixed(2) %></p>
      <p><strong>Lucro/Prejuízo atual (R$):</strong> <%= lucroPrejuizoAtual.toFixed(2) %></p>
    </div>

    <nav class="nav my-3">
      <a class="nav-link" href="/transactions">Transações</a>
      <a class="nav-link" href="/comparison">Comparar Plataformas</a>
      <a class="nav-link" href="/">Sair</a>
    </nav>
  </body>
</html>
